interface:
  display_name: "Codex Orchestration"
  short_description: "Optional plan advisor plus efficient executors"
  default_prompt: "Use $codex-orchestration with GPT-5.6 Luna at xhigh as executor and ask whether I want an optional advisor; keep my current session model as orchestrator."

policy:
  allow_implicit_invocation: false
